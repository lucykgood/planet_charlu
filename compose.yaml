services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    volumes:
      - .:/workspace
    ports:
      - "3001:3001"
    env_file:
      - .env
    stdin_open: true
    tty: true